/**
 * api接口
 * */
const baseUrl: string = `/api/`;//base接口
export const loginApi: string = `${baseUrl}login`;//登录
export const bindApi: string = `${baseUrl}bind`;//绑定
export const individualApi:string = `${baseUrl}individual`;//个人信息
export const removeBindApi:string = `${baseUrl}removeBind`;//解除绑定
export const examApi:string = `${baseUrl}exam`;//考试列表
export const chooseReportApi:string = `${baseUrl}chooseReport`;//报告列表
export const bureauEducationApi:string = `${baseUrl}report/bureauEducation`;//获取报告
